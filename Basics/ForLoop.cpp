#include <iostream>
int main(){
    // a for loop has three arguments:A variable that changes with every increment, a stopping condition , and an increment statement(Whether the counter should increase or decrease with every iteration).    
    for(int x=0;x>=-20;x--){
        std::cout<<x<<std::endl;

    }
    return 0;
}